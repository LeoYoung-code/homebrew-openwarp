# Homebrew OpenWarp Tap

Install OpenWarp with Homebrew:

```bash
brew tap LeoYoung-code/openwarp
brew install --cask openwarp
```

OpenWarp releases are published from https://github.com/LeoYoung-code/warp.
